# Content :
- ListNode 链表数据结构
- Number of bytes occupied by various data types. 各种数据类型所占的字节数
- The 'Pointer' Operations 指针操作 （通过修改指针指向的内存地址直接修改对应的值）
- The 'Class' and its Instance operate. 类与实例化
- How to use the 'struct' to custom yourself data types. 如何使用struct结构体构建自定义的数据结构
- The operation related to 'std::set<char>' 有关集合的操作（取出，存入）

# How to Run ?

  > # At the DataStruct dir:
  > @root~$  mkdir build && cd build && cmake .. && cmake --build . && ./DataStruct
